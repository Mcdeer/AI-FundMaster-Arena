# 总体介绍
本届 Vibe Coding 大赛致力于打造 AI 创新创意的选拔通道，为员工的奇思妙想提供被看见、被认可的舞台。无论你来自哪个岗位，只要你有好想法，就能借助 AI 工具将其落地为一个完整的作品。

同时，大赛也是一次 AI 能力的集中展现。我们期待看到每位参赛者如何与 AI 协作、如何用自然语言驱动代码生成，让 AI 真正成为每个人的创造伙伴。优秀作品不仅将在全公司面前亮相，更有机会评估转化为实际产品功能。

参赛对象
面向公司全员开放报名，solo或2-3人组队均可。

投稿形式
可访问的在线作品链接。

作品方向建议
公司业务相关， 例如 泛财经类应用

日常办公提效

希望有创造力的你，可以将AI能力融入真实业务或日常工作场景中。大胆抛出你的奇思妙想，这里有成千上万个声音，正准备为你疯狂呐喊！

报名及投稿
即日起启动报名，点击首页“我要报名”即可参与，快发动你的小伙伴一起来Vibe Coding吧！

若需提供部署资源，请在报名表中按需填写，便于后续高效支持。

报名通道已开启，点击「我要报名」即可参与，快来秀出你的最强脑洞，让东财见证你的高光时刻！




# 平台概述

AI创想家大赛平台构建了一个集中展示平台，用于呈现参赛者的创意作品，供用户进行体验与投票。为明确本次参赛作品的开发与投稿细则，请在投稿前仔细阅读本文档中的相关要求。

投稿作品提供方式说明

报名开启后，参赛者需登录平台完成注册，并在报名阶段选定最终作品体验环境的部署方案。为降低参赛门槛，本次大赛的作品演示环境支持两种部署方式。参赛者可根据自身条件及作品特性，选择适宜的方案。

方式A：参赛者自行提供作品的体验环境

本方式允许参赛者在完成作品开发后，自行部署并提供可访问的URL地址。该方式为参赛者提供了较高的灵活性与自主性，可根据自身技术背景选择合适的方式提供作品访问地址。例如：
- 常规用户：可利用AiWork等Agent平台提供的开放能力，在作品开发完成后，直接获取其生成的在线访问URL。
- 资深开发者：可在自有服务器资源上部署完整环境，并通过Nginx或SLB等负载均衡工具提供访问URL（请注意避免使用线上业务环境服务器）。
采用此方式，参赛者可在报名成功后即开始开发与部署工作。作品完成后，在平台提交环节仅需将最终作品的访问URL填入指定位置即可。


方式B：平台提供作品部署所需服务器资源

平台可为参赛作品提供部署环境所需的资源及相关支持。若需平台提供服务器资源进行部署，需在报名时同步登记并填写所需服务器资源信息。报名信息提交后，大赛平台将同步进行服务器资源的配置与准备工作。资源就绪后，平台将通过咚咚消息通知参赛者，届时参赛者可登录平台完成作品部署。


作品安全性保障

编码安全是研发过程中必须持续关注的核心要求，本次竞赛同样要求参赛者重视并确保所开发作品的安全性。所有投稿作品部署后，平台将联合安全部门进行全面的安全漏洞扫描，若发现严重问题，将通知参赛者并在规定期限内要求完成修复。
此外，在编码阶段，参赛者需遵循安全编码规范。为协助大家更好地保障源码安全性，平台与安全部门联合提供了源码扫描工具（security-skill），请参赛者下载并安装该工具，在编码完成前执行扫描，识别并修复源码中的安全隐患。

源码安全扫描技能使用说明
- Step 1： 下载本文档附件（security-skill.zip）至本地目录
- Step 2： 在您的AI Agent中安装此技能

- Step 3： 于AI Agent中执行源码扫描

- Step 4： 审查安全扫描结果并完成修复

平台可提供的资源说明

 如果您选择按照“方式A”自行提供作品的演示地址，无需关注此章节内容。
本次赛事提供两种规格的服务器供选择（每个作品可提供1台服务器），参赛者如有需要，可在报名阶段确定所需机型：
系统
规格
磁盘
所在机房
网络说明
Windows Server 2016
2C4G
40G
嘉定（独立网段）
具备公网访问权限，与公司周浦、浦江、嘉定（T/P）网络隔离。
CentOS7.9
2C4G
40G
嘉定（独立网段）
具备公网访问权限，与公司周浦、浦江、嘉定（T/P）网络隔离。

此外，平台还提供以下共享中间件资源，供参赛作品按需选用：
- MySQL 8.0.32
- Redis 7.2.3
- MongoDB 7.0.4
- Kafka 2.6.3
- Milvus 2.4.17

若有其他特殊依赖需求，可通过以下两种途径解决：
- 在分配的独立服务器上自行安装
- 备注于报名表中，平台将酌情评估

平台统一部署作品开发规范

如果您选择按照“方式A”自行提供作品的演示地址，无需关注此章节内容。
如果选择按照“方式B”由平台提供机器资源和自动部署支持，则作品必须遵循如下要求进行开发。
对外web服务参数约束

限定本次参赛所有作品的对外服务的参数约束如下（强制约束，必须遵循）：
- 对外服务端口： 21818（固定值）
eg： 保证web端访问的固定地址为： http://ip:21818/ 可以打开。

作品编码安全性保障要求

作品打包与自动部署脚本要求

为简化参赛作品的提交与部署流程，平台提供了一键部署功能。鉴于本次大赛对编程语言与作品形式不作限制，各作品的部署方式存在差异，参赛者需依据大赛规范，为每个作品自行准备安装、启停脚本，并按标准格式打包后上传。
平台提供了打包部署辅助工具包，作品开发完成后，可借助该工具包生成符合标准的部署包并提交。

安装与使用步骤：
- Step1：下载本文档附件（ai-creator-package-deploy.zip）至本地目录
- Step2：在您的AI Agent中安装此工具包


- Step3：执行此工具包，完成端口合规性校验，并编写安装、升级、启动、停止脚本，随后编译构建项目，最终按平台规范打包生成zip文件





- Step4：将作品上传至平台进行部署



Step5：部署完成后，通过平台界面生成的作品访问地址进行访问，以验证部署结果。

鉴于参赛作品的类型和语言无法被完全枚举和验证，若上述自动打包部署流程执行失败，参赛者可依据详细规则要求，指导其AI Agent参照平台打包规范，自行生成安装、升级、启动及停止脚本，并最终打包为符合要求的部署包。

打包部署安装规范要求： 

FAQ

Q1：作品提交后我还可以更新我的作品吗？

可以。 
平台希望您可以将自己的创意尽可能完美地呈现出来，所以不会限制你对作品的不断打磨与升级。比赛过程中，您可以根据实际情况，决定是否更新您的作品。但是请您保证更新之后的在线体验环境的稳定，如果更新导致作品在线体验地址不可访问等情况，可能会影响作品的得票哦。

Q2：我需要平台提供部署资源，如何才能连接到大赛提供的服务器上？

本次大赛分配的服务器资源位于公司的嘉定机房，本次所有服务器都在独立网段中，可以访问公网地址，但无法与公司的周浦、浦江以及嘉定T/P机房进行网络通信。
如果需要从办公网络访问服务器，需要开启无边界连接后方可访问。

Q3：我需要平台提供的部署资源，但能否自己登录服务器去手动部署？

可以的。
分配资源的时候，我们会提供分配给你的作品使用的服务器的IP与账号密码等信息。如果你本身就是研发人员，对服务器部署等操作比较熟悉，强烈推荐自行部署并调试保证功能正常。

Q4：投稿或者提交作品遇到问题找谁支撑？

如果对打包规则以及部署脚本的开发有疑问，可以联系 @杨博2 进行咨询。
如果部署作品到服务器上遇到问题，可以在作品部署界面下方，看到专属运维支撑人员，联系对应人员协助处理。

这里要特别说明一句： 盘中时间，我们的运维同学要优先保障线上服务的正常， 响应时间可能会有延时，请耐心等待。





# AI创想家大赛支持两种作品提交方式，具体信息请参阅。
本规范文档仅适用于采用“方式B：平台提供机器资源与部署支撑”的场景。选择方式B提交作品的参赛者，须严格遵循本文档所规定的相关规范与约束。

规范说明
参赛作品完成开发后，需整理为可独立部署的 ZIP 服务包。平台将 ZIP 服务包分发至已分配的 Linux（CentOS）或 Windows 服务器，执行文件完整性校验、服务包解压，并按预设顺序执行生命周期脚本。
本规范用于定义服务包的目录结构、生命周期脚本命名规范、脚本执行行为及文件安全要求，适用于 Java、Python、Node.js、Go、C/C++、.NET、静态网页及其他技术栈。
平台支持同一参赛项目多次上传与重新部署，每次部署均保留独立的发布记录及原始 ZIP 服务包，便于查看部署状态、失败原因及历史版本。
关键约束
部署平台与运行环境
提交服务包时，需选择与已分配服务器一致的目标操作系统：Linux（CentOS）或 Windows。服务包中的生命周期脚本必须与目标操作系统匹配。
目标服务器的操作系统及 Java、Python 3、Node.js 等基础运行环境，原则上应在部署前由资源维护人员完成准备。如需特定版本的运行环境或额外的系统软件、系统库，应提前联系资源维护人员处理。
端口校验
本次大赛要求参赛作品的对外 Web 服务使用固定端口 21818，打包前请务必核对校验，确保端口配置正确。
若端口配置错误，部署后将无法正常访问作品页面。
脚本要求
作品提交前，必须提供对应的安装（含升级操作）脚本，以及服务的启动、停止等操作脚本。
请注意，根据作品最终运行在 Windows 或 Linux（CentOS）系统的差异，需提供配套系统可执行的脚本。
平台
脚本要求
Windows
.bat 或 .py
Linux
.sh 或 .py
各脚本的具体要求如下详述。
Python 生命周期脚本
选择 Python 生命周期脚本模式时，install.py、start.py 和 stop.py 必须使用 Python 3 支持的语法，不支持 Python 2。
安装脚本（install）
安装脚本用于首次部署或重新部署时准备当前版本所需的项目依赖，也可执行应用升级前的数据或文件准备操作。
- Linux 原生脚本名称必须为 install.sh，Windows 原生脚本名称必须为 install.bat，Python 脚本名称必须为 install.py。
- 安装脚本为可选脚本。作品无需额外安装或升级操作时，可不提供。
- 安装脚本必须支持重复执行，已安装的依赖不应导致脚本执行失败。
- 依赖应安装在项目目录、项目虚拟环境或当前运行用户目录中，不得依赖管理员权限。
- 脚本正常完成时必须返回退出码 0；执行失败时返回非 0，并将明确的失败原因输出至标准错误流。
启动脚本（start）
启动脚本负责启动作品对外提供服务的进程。
- Linux 原生脚本名称必须为 start.sh，Windows 原生脚本名称必须为 start.bat，Python 脚本名称必须为 start.py。
- 启动脚本必须在确认服务已成功启动后返回退出码 0；启动失败时返回非 0。
- 服务进程必须在后台持续运行，启动脚本本身不能长期占用部署任务。建议在脚本中等待端口或健康检查接口可用后再正常退出。
- 启动脚本应避免重复启动同一服务。发现服务已运行时，应明确处理已有进程或安全退出。
- 应将关键启动过程及错误信息输出至控制台，便于部署失败时定位原因。
停止脚本（stop）
停止脚本负责停止当前参赛项目已运行的服务进程。
- Linux 原生脚本名称必须为 stop.sh，Windows 原生脚本名称必须为 stop.bat，Python 脚本名称必须为 stop.py。
- 首次部署时服务可能尚未运行，此时停止脚本也必须返回退出码 0，不得将“进程不存在”作为部署失败处理。
- 停止脚本只能停止当前作品自身的进程，不得使用范围过大的进程匹配条件影响其他作品。
- 重新部署时，平台执行的是新服务包中的停止脚本。因此停止脚本不能仅依赖上一版本解压目录中的临时文件，应使用稳定的项目标识、进程特征或当前用户可写的固定 PID 文件定位旧进程。
- 停止操作应等待进程真正退出；停止失败时返回非 0 并输出明确原因。
打包要求
ZIP 根目录结构
生命周期脚本必须直接位于 ZIP 根目录，不得先将整个项目目录打包至 ZIP，导致解压后脚本位于二级目录。
Linux Shell 模式示例：
service-package.zip
├── start.sh              必须
├── stop.sh               必须
├── install.sh            可选
├── app.jar               应用文件，名称不限
├── config/               可选业务目录
│   └── application.yml
└── static/               可选业务目录
    └── index.html
Windows BAT 模式示例：
service-package.zip
├── start.bat             必须
├── stop.bat              必须
├── install.bat           可选
├── app.jar               应用文件，名称不限
└── config/               可选业务目录
    └── application.yml
Python 生命周期脚本模式示例，Linux 和 Windows 均适用：
service-package.zip
├── start.py              必须
├── stop.py               必须
├── install.py            可选
├── requirements.txt      可选
└── src/                  应用文件目录
    └── main.py
生命周期脚本必须选择一种模式，不得混用：
- Linux：只能选择 start.sh + stop.sh，或选择 start.py + stop.py。
- Windows：只能选择 start.bat + stop.bat，或选择 start.py + stop.py。
- install 脚本若存在，必须与 start、stop 使用相同的脚本模式。
- 文件名必须使用上述小写名称，不支持自定义生命周期脚本名称。
文件与路径校验
上传的服务包必须同时满足以下要求：
- 文件格式必须为有效的 .zip 文件，文件名不能为空且长度不得超过 255 个字符。
- ZIP 文件大小必须大于 0，当前最大允许 500 MiB。
- ZIP 内文件和目录条目总数当前最多允许 10000 个。
- ZIP 解压后的文件总大小当前最多允许 2 GiB。
- ZIP 内不得存在重复文件路径。
- ZIP 内路径必须使用 / 分隔，不得使用 Windows 反斜杠 \。
- ZIP 内不得包含绝对路径、盘符、冒号或 .. 上级目录，以防止文件被解压至发布目录之外。
打包自检建议
上传前建议至少完成以下检查：
1. 在与目标服务器相同的操作系统中解压 ZIP，确认生命周期脚本位于解压根目录。
2. 手动依次执行 stop -> install -> start，确认全部脚本退出码正确。
3. 重复执行一次完整流程，确认安装、停止和启动脚本具备幂等性。
4. 检查应用实际监听端口是否为 21818，并从本机访问页面或健康检查接口。
5. 确认启动脚本退出后，应用进程仍正常运行。
6. 确认停止脚本不会误停止同一服务器上的其他应用。
7. 检查 ZIP 中不包含密钥、密码、个人令牌等敏感信息。
8. 删除缓存、日志、临时文件及无关构建目录，避免服务包过大。
部署流程与记录
部署执行顺序
平台接收服务包后，主要按照以下顺序执行部署：
1. 校验文件名、ZIP 格式、文件大小、目录路径及生命周期脚本。
2. 保存并解压服务包。
3. 执行停止脚本。
4. 若存在安装脚本，则执行安装脚本。
5. 执行启动脚本。
6. 全部脚本执行成功后，将本次发布标记为部署成功；任一步骤失败则记录失败信息。
生命周期脚本的固定执行顺序为：
stop -> install（如果存在） -> start
重新部署与历史记录
- 同一参赛项目支持多次上传与重新部署，每次上传均会生成新的发布版本及部署记录。
- 每次部署的原始 ZIP 会保留，可在平台中查看发布历史并下载历史服务包。
- 部署记录会显示排队、上传、分发、部署中、成功或失败等状态。
- 部署失败时，平台会返回失败阶段及具体失败信息，例如运行环境缺失、脚本退出码非 0、文件完整性校验失败或执行超时。